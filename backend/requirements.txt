Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Cors==4.0.1
# Flask-SocketIO: eliminado en Día 41 (SOCKET-17). Era código muerto —
# nunca tuvo manejadores ni emisiones; su único uso previsto lo cubre
# ahora Firebase Cloud Messaging. Ver nota en app/extensions.py.
psycopg2-binary==2.9.9
PyJWT==2.8.0
# Google Wallet API (firmado de pases y notificaciones de actualización)
google-auth==2.29.0
google-auth-httplib2==0.2.0
requests==2.31.0
bcrypt==4.1.3
redis==5.0.4
celery==5.4.0
python-dotenv==1.0.1
gunicorn==22.0.0
# eventlet: eliminado en Día 41 (SOCKET-17). Solo existía para el modo
# async de Flask-SocketIO. El Dockerfile nunca lo usó (gunicorn arranca
# sin '-k eventlet') y Celery usa el pool prefork por defecto.
webauthn==2.2.0
resend==2.0.0
marshmallow==3.21.2
# qrcode: eliminado en Día 31 — la tarjeta QR se genera en el cliente.
Pillow==10.3.0  # requerido por reportlab (recibos PDF) y opencv
opencv-python-headless==4.10.0.84

psutil==5.9.8
reportlab==4.2.5
Flask-Limiter==3.8.0
radon==6.0.1
firebase-admin==6.5.0
boto3==1.34.69  # DigitalOcean Spaces / AWS S3 compatible storage
